using System;
namespace DataCards.Models
{
    public static class SeedData
    {
        public static void Initialize(IServiceProvider serviceProvider)
        {
            /*
            // We need contexts for character attributes and wargear attributes.
            using (var context = new AttributesContext())
            {
                // If there is any attributes then the data has been seeded
                if (context.Attributes.Any?)
                {
                    return;
                }
            }
            */
        }
    }
}
